{
  "buildCommand": "cd react-portfolio && npm install && npx vite build",
  "outputDirectory": "react-portfolio/dist",
  "installCommand": "echo done",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
